## v0.5.1

### Highlights (EN)

- Service (macOS): suppress misleading `launchctl bootstrap ... Input/output error (5)` output during `clipal service restart` when the LaunchAgent is already loaded; restart behavior is unchanged.

### 亮点 (中文)

- 服务（macOS）：`clipal service restart` 时若 LaunchAgent 已处于加载状态，不再输出容易误导的 `launchctl bootstrap ... Input/output error (5)`；重启行为不变。
